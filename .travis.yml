language: python
python:
  - 2.7
  - 3.3
  - 3.4

install:
  - pip install -r requirements.txt
  - pip install -r requirements.dev.txt

script:
  - pylama tabulator
  - nosetests tests -sv --with-coverage --cover-package tabulator

after_success:
  - coveralls
