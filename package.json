{
  "name": "tabulator-py",
  "description": "A utility library that provides a consistent interface for reading tabular data.",
  "repository": "https://github.com/okfn/tabulator-py",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "^4.1.0",
    "npm": "^2.14.0"
  },
  "scripts": {
    "check": "npm run review && npm run test",
    "review": "pylama tabulator",
    "test": "nosetests tests --with-coverage --cover-package tabulator"
  },
  "dependencies": {
  }
}
